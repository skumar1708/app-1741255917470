const mongoose = require('mongoose'); const gameSchema = new mongoose.Schema({ board: { type: [[String]], default: Array(3).fill(null).map(() => Array(3).fill(null)) }, playerTurn: { type: String, default: 'X' }, status: { type: String, default: 'ongoing' }, winner: { type: String, default: '' }, createdAt: { type: Date, default: Date.now } }); module.exports = mongoose.model('Game', gameSchema);